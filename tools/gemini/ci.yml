gemini_ecb_cbc_it_build:
  stage: build
  script:
    - cd examples/peripheral/crypt/ecb_cbc_it && scons ic=gemini

gemini_ecb_cbc_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/crypt/ecb_cbc_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ecb_cbc_it.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_ecb_cbc_polling_build:
  stage: build
  script:
    - cd examples/peripheral/crypt/ecb_cbc_polling && scons ic=gemini

gemini_ecb_cbc_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/crypt/ecb_cbc_polling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ecb_cbc_polling.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_ecb_it_padding_build:
  stage: build
  script:
    - cd examples/peripheral/crypt/ecb_it_padding && scons ic=gemini

gemini_ecb_it_padding_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/crypt/ecb_it_padding/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ecb_it_padding.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_sm4_polling_build:
  stage: build
  script:
    - cd examples/peripheral/sm4/sm4_polling && scons ic=gemini

gemini_sm4_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/sm4/sm4_polling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b sm4_polling.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_sm4_it_build:
  stage: build
  script:
    - cd examples/peripheral/sm4/sm4_it && scons ic=gemini

gemini_sm4_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/sm4/sm4_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b sm4_it.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_touchkey_polling_build:
  stage: build
  script:
    - cd examples/peripheral/touchkey/touchkey_polling && scons ic=gemini

gemini_touchkey_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/touchkey/touchkey_polling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b touchkey_polling.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_touchkey_it_build:
  stage: build
  script:
    - cd examples/peripheral/touchkey/touchkey_it && scons ic=gemini

gemini_touchkey_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/touchkey/touchkey_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b touchkey_it.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_gpio_build:
  stage: build
  script:
    - cd examples/peripheral/gpio && scons ic=gemini

gemini_gpio_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/gpio/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b gpio.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_i2c_it_build:
  stage: build
  script:
    - cd examples/peripheral/i2c/i2c_it && scons ic=gemini

gemini_i2c_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/i2c/i2c_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b i2c_it.uvprojx -o UVBuild/build.txt -j0


gemini_ll_i2c_it_build:
  stage: build
  script:
    - cd examples/peripheral/i2c/ll_i2c_it && scons ic=gemini

gemini_ll_i2c_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/i2c/ll_i2c_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ll_i2c_it.uvprojx -o UVBuild/build.txt -j0

gemini_i2c_polling_build:
  stage: build
  script:
    - cd examples/peripheral/i2c/i2c_polling && scons ic=gemini

gemini_i2c_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/i2c/i2c_polling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b i2c_polling.uvprojx -o UVBuild/build.txt -j0

gemini_i2c_dma_build:
  stage: build
  script:
    - cd examples/peripheral/i2c/i2c_dma && scons ic=gemini

gemini_i2c_dma_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/i2c/i2c_dma/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b i2c_dma.uvprojx -o UVBuild/build.txt -j0

# gemini_pdm_build:
#   stage: build
#   script:
#     - cd examples/peripheral/pdm && scons ic=gemini

# gemini_pdm_keil_build:
#   stage: build
#   rules:
#     - !reference [.release_rule, rules]
#   script:
#     - cd examples/peripheral/pdm/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b pdm.uvprojx -o UVBuild/build.txt -j0

gemini_software_calendar_build:
  stage: build
  script:
    - cd examples/peripheral/software_calendar && scons ic=gemini

gemini_software_calendar_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/software_calendar/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b software_calendar.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_software_timers_build:
  stage: build
  script:
    - cd examples/peripheral/software_timers && scons ic=gemini

gemini_software_timers_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/software_timers/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b software_timers.uvprojx -o UVBuild/build.txt -j0 || type UVBuild/build.txt

gemini_ll_spi_it_build:
  stage: build
  script:
    - cd examples/peripheral/spi/ll_spi_it && scons ic=gemini

gemini_ll_spi_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/ll_spi_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ll_spi_it.uvprojx -o UVBuild/build.txt -j0

gemini_ll_spi_polling_build:
  stage: build
  script:
    - cd examples/peripheral/spi/ll_spi_polling && scons ic=gemini

gemini_ll_spi_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/ll_spi_polling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ll_spi_polling.uvprojx -o UVBuild/build.txt -j0

gemini_spi_dma_master_build:
  stage: build
  script:
    - cd examples/peripheral/spi/spi_dma_master && scons ic=gemini

gemini_spi_dma_master_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/spi_dma_master/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b spi_dma_master.uvprojx -o UVBuild/build.txt -j0

gemini_spi_dma_slave_build:
  stage: build
  script:
    - cd examples/peripheral/spi/spi_dma_slave && scons ic=gemini

gemini_spi_dma_slave_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/spi_dma_slave/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b spi_dma_slave.uvprojx -o UVBuild/build.txt -j0

gemini_spi_it_master_build:
  stage: build
  script:
    - cd examples/peripheral/spi/spi_it_master && scons ic=gemini

gemini_spi_it_master_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/spi_it_master/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b spi_it_master.uvprojx -o UVBuild/build.txt -j0

gemini_spi_it_slave_build:
  stage: build
  script:
    - cd examples/peripheral/spi/spi_it_slave && scons ic=gemini

gemini_spi_it_slave_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/spi_it_slave/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b spi_it_slave.uvprojx -o UVBuild/build.txt -j0

gemini_spi_polling_master_build:
  stage: build
  script:
    - cd examples/peripheral/spi/spi_polling_master && scons ic=gemini

gemini_spi_polling_master_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/spi_polling_master/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b spi_polling_master.uvprojx -o UVBuild/build.txt -j0

gemini_spi_polling_slave_build:
  stage: build
  script:
    - cd examples/peripheral/spi/spi_polling_slave && scons ic=gemini

gemini_spi_polling_slave_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/spi/spi_polling_slave/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b spi_polling_slave.uvprojx -o UVBuild/build.txt -j0

gemini_ll_ssi_it_build:
  stage: build
  script:
    - cd examples/peripheral/ssi/ll_ssi_it && scons ic=gemini

gemini_ll_ssi_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/ssi/ll_ssi_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ll_ssi_it.uvprojx -o UVBuild/build.txt -j0

gemini_ssi_dma_build:
  stage: build
  script:
    - cd examples/peripheral/ssi/ssi_dma && scons ic=gemini

gemini_ssi_dma_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/ssi/ssi_dma/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ssi_dma.uvprojx -o UVBuild/build.txt -j0

gemini_ssi_it_build:
  stage: build
  script:
    - cd examples/peripheral/ssi/ssi_it && scons ic=gemini

gemini_ssi_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/ssi/ssi_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ssi_it.uvprojx -o UVBuild/build.txt -j0

gemini_Basic_PWM_build:
  stage: build
  script:
    - cd examples/peripheral/timer/Basic_PWM && scons ic=gemini

gemini_Basic_PWM_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/timer/Basic_PWM/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b Basic_PWM.uvprojx -o UVBuild/build.txt -j0

gemini_Basic_TIM_build:
  stage: build
  script:
    - cd examples/peripheral/timer/Basic_TIM && scons ic=gemini

gemini_Basic_TIM_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/timer/Basic_TIM/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b Basic_TIM.uvprojx -o UVBuild/build.txt -j0

gemini_DTC_PWM_build:
  stage: build
  script:
    - cd examples/peripheral/timer/DTC_PWM && scons ic=gemini

gemini_DTC_PWM_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/timer/DTC_PWM/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b DTC_PWM.uvprojx -o UVBuild/build.txt -j0

gemini_input_Capture_build:
  stage: build
  script:
    - cd examples/peripheral/timer/input_Capture && scons ic=gemini

gemini_input_Capture_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/timer/input_Capture/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b input_Capture.uvprojx -o UVBuild/build.txt -j0

gemini_uart_dma_build:
  stage: build
  script:
    - cd examples/peripheral/uart/uart_dma && scons ic=gemini

gemini_uart_dma_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/uart/uart_dma/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b uart_dma.uvprojx -o UVBuild/build.txt -j0

gemini_uart_it_build:
  stage: build
  script:
    - cd examples/peripheral/uart/uart_it && scons ic=gemini

gemini_uart_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/uart/uart_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b uart_it.uvprojx -o UVBuild/build.txt -j0

gemini_ll_uart_it_build:
  stage: build
  script:
    - cd examples/peripheral/uart/ll_uart_it && scons ic=gemini

gemini_ll_uart_it_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/uart/ll_uart_it/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ll_uart_it.uvprojx -o UVBuild/build.txt -j0

gemini_ll_uart_polling_build:
  stage: build
  script:
    - cd examples/peripheral/uart/ll_uart_polling && scons ic=gemini

gemini_ll_uart_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/uart/ll_uart_polling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b ll_uart_polling.uvprojx -o UVBuild/build.txt -j0

gemini_uart_polling_build:
  stage: build
  script:
    - cd examples/peripheral/uart/uart_polling && scons ic=gemini

gemini_uart_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/uart/uart_polling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b uart_polling.uvprojx -o UVBuild/build.txt -j0

gemini_adc_amic_sampling_build:
  stage: build
  script:
    - cd examples/peripheral/adcv2/adc_amic_sampling && scons ic=gemini

gemini_adc_amic_sampling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/adcv2/adc_amic_sampling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b adc_amic_sampling.uvprojx -o UVBuild/build.txt -j0

gemini_adc_polling_build:
  stage: build
  script:
    - cd examples/peripheral/adcv2/adc_blocking_sampling && scons ic=gemini

gemini_adc_polling_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/adcv2/adc_blocking_sampling/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b adc_blocking_sampling.uvprojx -o UVBuild/build.txt -j0

gemini_adc_single_build:
  stage: build
  script:
    - cd examples/peripheral/adcv2/adc_single_channel && scons ic=gemini

gemini_adc_single_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/adcv2/adc_single_channel/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b adc_single_channel.uvprojx -o UVBuild/build.txt -j0

gemini_adc_multi_build:
  stage: build
  script:
    - cd examples/peripheral/adcv2/adc_multi_channel && scons ic=gemini

gemini_adc_multi_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/adcv2/adc_multi_channel/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b adc_multi_channel.uvprojx -o UVBuild/build.txt -j0

gemini_adc_analog_wdg_build:
  stage: build
  script:
    - cd examples/peripheral/adcv2/adc_analog_wdg && scons ic=gemini

gemini_adc_analog_wdg_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/adcv2/adc_analog_wdg/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b adc_analog_wdg.uvprojx -o UVBuild/build.txt -j0

gemini_adc_dma_build:
  stage: build
  script:
    - cd examples/peripheral/adcv2/adc_multi_channel_dma && scons ic=gemini

gemini_adc_dma_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/adcv2/adc_multi_channel_dma/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b adc_multi_channel_dma.uvprojx -o UVBuild/build.txt -j0

gemini_dual_dac_channel_dma_voltage_build:
  stage: build
  script:
    - cd examples/peripheral/dac/dual_dac_channel_dma_voltage && scons ic=gemini

gemini_dual_dac_channel_dma_voltage_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/dac/dual_dac_channel_dma_voltage/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b dual_dac_channel_dma_voltage.uvprojx -o UVBuild/build.txt -j0

gemini_dual_dac_channel_voltage_build:
  stage: build
  script:
    - cd examples/peripheral/dac/dual_dac_channel_voltage && scons ic=gemini

gemini_dual_dac_channel_voltage_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/dac/dual_dac_channel_voltage/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b dual_dac_channel_voltage.uvprojx -o UVBuild/build.txt -j0

gemini_dual_dac_channel_wave_build:
  stage: build
  script:
    - cd examples/peripheral/dac/dual_dac_channel_wave && scons ic=gemini

gemini_dual_dac_channel_wave_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/dac/dual_dac_channel_wave/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b dual_dac_channel_wave.uvprojx -o UVBuild/build.txt -j0

gemini_single_dac_channel_dma_voltage_build:
  stage: build
  script:
    - cd examples/peripheral/dac/single_dac_channel_dma_voltage && scons ic=gemini

gemini_single_dac_channel_dma_voltage_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/dac/single_dac_channel_dma_voltage/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b single_dac_channel_dma_voltage.uvprojx -o UVBuild/build.txt -j0

gemini_single_dac_channel_sine_wave_build:
  stage: build
  script:
    - cd examples/peripheral/dac/single_dac_channel_sine_wave && scons ic=gemini

gemini_single_dac_channel_sine_wave_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/dac/single_dac_channel_sine_wave/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b single_dac_channel_sine_wave.uvprojx -o UVBuild/build.txt -j0

gemini_single_dac_channel_voltage_build:
  stage: build
  script:
    - cd examples/peripheral/dac/single_dac_channel_voltage && scons ic=gemini

gemini_single_dac_channel_voltage_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/dac/single_dac_channel_voltage/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b single_dac_channel_voltage.uvprojx -o UVBuild/build.txt -j0

gemini_single_dac_channel_wave_build:
  stage: build
  script:
    - cd examples/peripheral/dac/single_dac_channel_wave && scons ic=gemini

gemini_single_dac_channel_wave_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/dac/single_dac_channel_wave/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b single_dac_channel_wave.uvprojx -o UVBuild/build.txt -j0

gemini_rtc_build:
  stage: build
  script:
    - cd examples/peripheral/rtcv2 && scons ic=gemini

gemini_rtc_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/rtcv2/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b rtcv2.uvprojx -o UVBuild/build.txt -j0

gemini_can_loopback_build:
  stage: build
  script:
    - cd examples/peripheral/can/CAN_LoopBack && scons ic=gemini

gemini_can_loopback_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/can/CAN_LoopBack/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b CAN_LoopBack.uvprojx -o UVBuild/build.txt -j0

gemini_can_normal_build:
  stage: build
  script:
    - cd examples/peripheral/can/CAN_Normal && scons ic=gemini

gemini_can_loopback_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/can/CAN_Normal/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b CAN_Normal.uvprojx -o UVBuild/build.txt -j0

gemini_iwdg_build:
  stage: build
  script:
    - cd examples/peripheral/wdg/iwdg && scons ic=gemini

gemini_iwdg_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/wdg/iwdg/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b iwdg.uvprojx -o UVBuild/build.txt -j0

gemini_wwdg_build:
  stage: build
  script:
    - cd examples/peripheral/wdg/wwdg && scons ic=gemini

gemini_wwdg_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/wdg/wwdg/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b wwdg.uvprojx -o UVBuild/build.txt -j0

gemini_comp_internal_build:
  stage: build
  script:
    - cd examples/peripheral/comp/comp_internal && scons ic=gemini

gemini_comp_internal_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/comp/comp_internal/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b comp_internal.uvprojx -o UVBuild/build.txt -j0
    
gemini_comp_dac_build:
  stage: build
  script:
    - cd examples/peripheral/comp/comp_dac && scons ic=gemini

gemini_comp_dac_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/comp/comp_dac/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b comp_dac.uvprojx -o UVBuild/build.txt -j0

gemini_iap_bootloader_build:
  stage: build
  script:
    - cd examples/peripheral/iap/iap_bootloader && scons ic=gemini

gemini_iap_bootloader_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/iap/iap_bootloader/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b iap_bootloader.uvprojx -o UVBuild/build.txt -j0

gemini_iap_application_build:
  stage: build
  script:
    - cd examples/peripheral/iap/iap_application && scons ic=gemini

gemini_iap_application_keil_build:
  stage: build
  rules:
    - !reference [.release_rule, rules]
  script:
    - cd examples/peripheral/iap/iap_application/gemini/uvision5/ && C:/Keil_v5/UV4/UV4.exe -b iap_application.uvprojx -o UVBuild/build.txt -j0
